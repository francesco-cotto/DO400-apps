pipeline{
    agent any

    stages{
        stage('Deploy') {
            steps {
                sh '''
                    oc project xtzhdd-deploy-strategies
                    oc start-build greeting-service --follow --wait
                '''
            }
        }
    }
}
